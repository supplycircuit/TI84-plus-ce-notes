Let $P_3$ have inner product
$$\langle p,q \rangle = \int_{-1}^{1} p(x)q(x)\,dx.$$

For $p=2x^3$ and $q=1-x^3$, compute:
1. $\langle p,q \rangle$
2. $d(p,q)$
3. $\|p\|$
4. $\|q\|$

Cosine between matrices with Frobenius inner product:
$$\cos \theta = \frac{\langle A,B \rangle}{\|A\|\|B\|}.$$
