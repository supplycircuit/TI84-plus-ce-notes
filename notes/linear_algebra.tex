Find the characteristic equation, the eigenvalues, and bases for the eigenspaces of
$$\begin{bmatrix} 1 & -3 & 3 \\ 3 & -5 & 3 \\ 6 & -6 & 4 \end{bmatrix}$$

Find a matrix $P$ that diagonalizes $A$, and check by computing $P^{-1}AP$.

Spectral decomposition template:
1. Solve $\det(A-\lambda I)=0$.
2. Compute eigenvectors for each eigenvalue.
3. Build $Q$ from orthonormal eigenvectors and diagonal $\Lambda$.
4. Verify $A = Q\Lambda Q^T$.
